trigger:
- master

pool:
  name: Pool-test

# ETAPA DE CI

stages:
  - stage: Build
    displayName: 'CI'
    jobs:
    - job: Scripts
      displayName: 'Scripts'
      steps:
#      - checkout: self
      - script: |
          echo $(Build.SourcesDirectory)

# ETAPA DE CD

#  - stage: Deploy
#    displayName: 'Deploy Stage'
#    jobs:
#    - job: DeployJob
#      displayName: 'Desplegando'
#      steps:
